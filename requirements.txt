# Core ML runtime. Install PyTorch with CUDA SEPARATELY first (see SETUP.md), e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# then install the rest with this file.
transformers>=4.44
accelerate>=0.33
bitsandbytes>=0.43        # 4-bit quantization (Linux / WSL2)
# NOTE: banking77 is read from the canonical CSVs in data/ (see SETUP.md), not via a
# `datasets` script loader — datasets>=4.0 no longer executes dataset scripts.

# Analysis + calibration
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.8

# Serving
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0

# Tests
pytest>=8.0
